---
import { Image } from 'astro:assets';
import meridianImg from '../../assets/images/index/meridian_thumb.png';
import stratumImg from '../../assets/images/index/stratum_thumb.png';
import stratumHero from '../../assets/images/index/stratum_thumb.png';
---
<section id="projects">
    <h2><span>Projects</span></h2>

    <div class="project-entry">
        <a href="/projects/stratum" class="project-link">
            <div class="project-image">
                <Image 
                    src={stratumImg} 
                    alt="Stratum"
                    quality={100}
                    format="webp"
                    loading="eager"
                />
            </div>
            <div class="project-title">
                <h3>Stratum: Kroger's Analytics Platform</h3>
                <p class="label">84.51°</p>
            </div>
            <div class="project-content">
                <p>Designed an analytics platform from the ground up that transforms Kroger shopper data from 62M+ households into actionable insights for our partners. Since launch, we've generated over $1B in contracts through intuitive UI design that makes complex retail data accessible at scale.</p>
            </div>
        </a>
    </div>

    <div class="project-entry">
        <a href="/projects/meridian" class="project-link">
            <div class="project-image">
                <Image 
                    src={meridianImg} 
                    alt="Meridian Design System"
                    quality={100}
                    format="webp"
                    loading="eager"
                />
            </div>
            <div class="project-title">
                <h3>Meridian Design System</h3>
                <p class="label">84.51°</p>
            </div>
            <div class="project-content">
                <p>Maintaining and scaling a proprietary design system across Kroger data enterprise products to serve thousands of customers.</p>
            </div>
        </a>
    </div>

</section>

<style lang="scss">
    .project-entry {
        display: flex;
        flex-direction: column;
        gap: var(--size-md);
        --hover-area: var(--size-sm);
        padding: var(--hover-area);
        margin: calc(-1 * var(--hover-area));
        margin-bottom: var(--size-xl);
        border-radius: var(--border-radius-md);
        cursor: pointer;
        transition: background-color var(--transition-default);

        &:first-child {
            margin-top: var(--size-xl);
        }
        
        &:last-child {
            margin-bottom: 0;
        }
        
        &:hover {
            background-color: var(--color-surface-hover);
        }
        
        .project-image {
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            cursor: pointer;
            
            img {
                width: 100%;
                height: auto;
                display: block;
                image-rendering: -webkit-optimize-contrast;
                transition: filter 0.3s ease;
            }
        }

        .project-title {
            display: flex;
            flex-direction: column;
            gap: var(--size-xxs);
        }
        
        .project-content {
            display: flex;
            flex-direction: column;
            gap: var(--size-xs);
            
            h3 {
                color: var(--color-ink-primary);
                
                &:hover {
                    color: var(--color-ink);
                }
            }
        }

        .project-link {
            text-decoration: none;
            color: inherit;
            display: flex;
            flex-direction: column;
            gap: var(--size-md);
        }
    }
</style>